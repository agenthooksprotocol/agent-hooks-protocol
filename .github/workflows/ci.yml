name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  conformance:
    name: Conformance artifacts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate schemas, fixtures, and manifests
        run: python3 tools/check_conformance.py
