{
  "protocolVersion": "draft",
  "hooks": [
    {
      "id": "com.example.observer",
      "transport": {"type": "stdio", "command": "observer", "lifecycle": "per_event"},
      "subscriptions": [
        {"events": ["session.start"], "mode": "observe", "timeoutMs": 100, "failurePolicy": "fail-open"}
      ]
    }
  ]
}
